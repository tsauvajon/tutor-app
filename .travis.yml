language: node_js
node_js:
  - "6"

cache: yarn

env:
  global:
    - CI: true
    - NODE_ENV: "production"

script:
  - yarn build

after_success:
  - yarn global add firebase-tools
  - firebase deploy --token
